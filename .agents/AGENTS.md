# Agent Behaviors & Rules

- **Auto-commit & Push**: Automatically stage (`git add .`), commit, and push all file modifications, code additions, or removals directly to the Git repository after every meaningful set of changes without asking for the user's explicit permission.
